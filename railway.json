{
    "build": {
        "env": {},
        "commands": [
            "composer install",
            "php artisan key:generate",
            "php artisan migrate --force"
        ]
    },
    "start": {
        "cmd": "php -S 0.0.0.0:$PORT -t public"
    }
}